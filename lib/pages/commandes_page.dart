import 'package:flutter/material.dart';

class CommandesPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Bienvenue sur la page CommandesPage', style: TextStyle(fontSize: 24)),
    );
  }
}
